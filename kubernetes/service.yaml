apiVersion: v1
kind: Service
metadata:
  name: demo-api-service
  namespace: demo-api-dev
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: demo-api
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007
