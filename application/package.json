{
  "name": "demo-api",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal HTTP API for CI/CD + K8s portfolio",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/smoke.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
